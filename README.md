# oring-inspection
B00136730 Yurie Bohdan

Run it
python src/main.py

The program loads the O-ring images, performs thresholding, morphology, connected component labeling and shows if each O-ring as PASS or FAIL.

